spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      username: config-user
      password: config-pass
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6
        