spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8888
      username: config-user
      password: config-pass
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6